dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install g++-4.9
    - sudo apt-get install gcc-4.9
    - sudo apt-get install libstdc++-4.8-dev

compile:
  override:
    - make -C flappybird/